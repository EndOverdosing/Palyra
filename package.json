{
    "name": "peer2peer-chat",
    "version": "1.0.0",
    "scripts": {
        "dev": "vite",
        "build": "chmod +x node_modules/.bin/vite && vite build"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    }
}